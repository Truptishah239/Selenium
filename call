from selenium import webdriver
PATH = "C:\Users\v-trugan\AppData\Roaming\Microsoft\Windows\Start Menu\Programs\chromedriver.exe"
driver = webdriver.Chrome(PATH)
from selenium.webdriver.common.keys import keys
driver.get("https://teams.microsoft.com/")
print(driver.title)

from selenium import webdriver
from getpass import getpass
driver = webdriver.Chrome(Path)
driver.get("https://www.office.com/")
elem = driver.find_element_by_link_text('Sign in')
elem.click()
elem = driver.find_element_by_id('hero-banner-sign-back-in-to-office-365-link')
elem.click()

elem2= driver2.find_element_by_class_name("cb")
elem2.send_keys(usename_textbox )

usename_textbox = input("v-trugan@microsoft.com")
password_textbox  = getpass("Enter your password: ")


inputboxes = driver.find_elements(By.CLASS_NAME,'cb')
print(len(inputboxes))

usename_textbox = input("v-trugan@microsoft.com")
password_textbox  = getpass("Enter your password: ")
elem2= driver2.find_element_by_class_name("cb")
elem2.send_keys(usename_textbox )

elem = driver.find_element_by_class_name('mectrl_profilepic mectrl_glyph glyph_signIn_circle x-hidden-focus')
elem.click()

status = driver.find_element_by_class_name('cb').is_displayed()
print("displayed or not: ", status)

status = driver.find_element_by_class_name('cb').is_enabled()
print("Enabled or not: ", status)

